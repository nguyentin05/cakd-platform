package registry

// Provider and technology constants used across the registry as SSOT keys.
const (
	PostgreSQL = "postgresql"
	MySQL      = "mysql"
	Redis      = "redis"
	RabbitMQ   = "rabbitmq"
	Prometheus = "prometheus"
)
